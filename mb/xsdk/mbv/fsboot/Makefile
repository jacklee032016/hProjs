# Auto Generated by Xilinx generate_app. Modify at your own risk

CC := mb-gcc
CC_FLAGS := -MMD -MP -mlittle-endian -mno-xl-soft-mul -mxl-multiply-high -mxl-barrel-shift -mxl-pattern-compare -mno-xl-soft-div -mcpu=v10.0    
CFLAGS := 
LN_FLAGS := -Wl,--start-group,-lxil,-lgcc,-lc,--end-group  -Wl,--gc-sections

BSP_HOME:=../ws/bspProj

c_SOURCES := $(wildcard *.c)
S_SOURCES := $(wildcard *.S)
s_SOURCES := $(wildcard *.s)
INCLUDES := $(wildcard *.h)
OBJS := $(patsubst %.c, %.o, $(c_SOURCES))
OBJS += $(patsubst %.S, %.o, $(S_SOURCES))
OBJS += $(patsubst %.s, %.o, $(s_SOURCES))
#LSCRIPT := -Tlscript.ld
LSCRIPT := -Tlscript.bram.ld

CURRENT_DIR = $(shell pwd)
DEPFILES := $(patsubst %.o, %.d, $(OBJS))
LIBS := $(BSP_HOME)/microblaze_0/lib/libxil.a
EXEC := fsboot.elf

INCLUDEPATH := -I$(BSP_HOME)/microblaze_0/include -I.
LIBPATH := -L$(BSP_HOME)/microblaze_0/lib

all: $(EXEC)

$(EXEC): $(LIBS) $(OBJS) $(INCLUDES)
	$(CC) -o $@ $(OBJS) $(CC_FLAGS) $(CFLAGS) $(LN_FLAGS) $(LIBPATH) $(LSCRIPT)

$(LIBS):
	$(MAKE) -C $(BSP_HOME)

%.o:%.c
	$(CC) $(CC_FLAGS) $(CFLAGS) -c $< -o $@ $(INCLUDEPATH)

%.o:%.S
	$(CC) $(CC_FLAGS) $(CFLAGS) -c $< -o $@ $(INCLUDEPATH)

%.o:%.s
	$(CC) $(CC_FLAGS) $(CFLAGS) -c $< -o $@ $(INCLUDEPATH)

clean:
	rm -rf $(OBJS) $(LIBS) $(EXEC) *.o

-include $(DEPFILES)
